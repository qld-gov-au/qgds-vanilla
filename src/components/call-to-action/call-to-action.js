/**
 * Call to Action component logic attached to an init() function.
 * This logic is imported by the component's entry file (index.js) and ships with the component.
 */

export default {
  init() {},
};
