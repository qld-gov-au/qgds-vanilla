{{! modal example test wrapper }}
{{#if items}}
    {{#each items}}
        <!--  {{ component.type}}  -->
        {{> (lookup component "type") this }}
    {{/each}}
{{else}}
    {{> (lookup component "type") this }}
{{/if}}

<br>
<br>
<br>
<br>
<br>
